/* eslint-disable jsdoc/require-jsdoc -- Internal helpers are self-descriptive */
import { isValidJson } from '@metamask/utils';
import { RPCInvokeMethodErr } from '../../domain';
const MAX_ERROR_CAUSE_DEPTH = 5;
function getErrorObject(value) {
    if (typeof value === 'object' && value !== null) {
        return value;
    }
    return undefined;
}
function getNumericCode(value) {
    return typeof value === 'number' ? value : undefined;
}
function getNonEmptyMessage(value) {
    return typeof value === 'string' && value.length > 0 ? value : undefined;
}
function getJsonData(value) {
    return value !== undefined && isValidJson(value) ? value : undefined;
}
function getFirstNonEmptyMessage(values) {
    for (const value of values) {
        const message = getNonEmptyMessage(value);
        if (message !== undefined) {
            return message;
        }
    }
    return undefined;
}
function getErrorObjectChain(errorObject) {
    const chain = [];
    let currentObject = errorObject;
    for (let depth = 0; currentObject !== undefined && depth < MAX_ERROR_CAUSE_DEPTH; depth += 1) {
        chain.push(currentObject);
        currentObject = getErrorObject(currentObject.cause);
    }
    return chain;
}
function getCodedErrorDetails(value) {
    const code = getNumericCode(value === null || value === void 0 ? void 0 : value.code);
    if (code === undefined) {
        return undefined;
    }
    const message = getNonEmptyMessage(value === null || value === void 0 ? void 0 : value.message);
    const data = getJsonData(value === null || value === void 0 ? void 0 : value.data);
    return Object.assign(Object.assign({ code }, (message === undefined ? {} : { message })), (data === undefined ? {} : { data }));
}
/**
 * Extracts the public invocation error fields from either a JSON-RPC error
 * payload or a rejected Error-like value.
 *
 * @param error - Unknown error thrown or returned during method execution.
 * @returns Canonical fields for RPCInvokeMethodErr.
 */
function getInvocationErrorDetails(error) {
    var _a, _b, _c, _d;
    const errorObject = getErrorObject(error);
    const errorObjectChain = getErrorObjectChain(errorObject);
    const primitiveMessage = getNonEmptyMessage(error);
    for (const [index, currentObject] of errorObjectChain.entries()) {
        const codedDetails = getCodedErrorDetails(currentObject);
        if (codedDetails) {
            // The rpc* fields mirror the wallet's own JSON-RPC error, so they are
            // sourced strictly from the coded node. The human-readable `reason` may
            // still fall back through the cause chain (deeper cause first, then outer
            // wrappers) so logs stay descriptive when the wallet omits a message.
            const descendantObjects = errorObjectChain.slice(index + 1);
            const ancestorObjects = errorObjectChain.slice(0, index);
            const descendantMessage = getFirstNonEmptyMessage(descendantObjects.map((object) => object.message));
            const ancestorMessage = getFirstNonEmptyMessage([
                primitiveMessage,
                ...ancestorObjects.map((object) => object.message),
            ]);
            const reason = (_c = (_b = (_a = codedDetails.message) !== null && _a !== void 0 ? _a : descendantMessage) !== null && _b !== void 0 ? _b : ancestorMessage) !== null && _c !== void 0 ? _c : 'Unknown error';
            return Object.assign(Object.assign({ reason, rpcCode: codedDetails.code }, (codedDetails.message === undefined
                ? {}
                : { rpcMessage: codedDetails.message })), (codedDetails.data === undefined
                ? {}
                : { rpcData: codedDetails.data }));
        }
    }
    const reason = (_d = primitiveMessage !== null && primitiveMessage !== void 0 ? primitiveMessage : getFirstNonEmptyMessage(errorObjectChain.map((object) => object.message))) !== null && _d !== void 0 ? _d : 'Unknown error';
    return {
        reason,
    };
}
/**
 * Normalizes unknown invocation errors to the router error type.
 *
 * @param error - Unknown error thrown or returned during method execution.
 * @returns Error instance surfaced by invokeMethod.
 */
export function toRPCInvokeMethodErr(error) {
    if (error instanceof RPCInvokeMethodErr) {
        return error;
    }
    const { reason, rpcCode, rpcMessage, rpcData } = getInvocationErrorDetails(error);
    return new RPCInvokeMethodErr(reason, rpcCode, rpcMessage, rpcData);
}
//# sourceMappingURL=invocationError.js.map